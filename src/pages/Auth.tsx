
import { AuthForm } from "@/components/AuthForm";

export default function Auth() {
  return (
    <div className="min-h-screen flex items-center justify-center p-4 bg-linkbloom-primary/10">
      <AuthForm />
    </div>
  );
}
